# SDE importer deps.
#
# Pinned to minor — these are stable libraries and we want reproducible
# image builds, but patch bumps for security fixes are fine.
httpx~=0.27
pymysql~=1.1
python-ulid~=3.0
# python-ulid 3.0.x unconditionally imports `Self` from typing_extensions
# but only declares the dep on Python < 3.11; on the 3.12 base image the
# import fails at module load. Pin it explicitly until upstream is fixed.
typing_extensions>=4.7
