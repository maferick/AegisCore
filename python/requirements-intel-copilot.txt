# intel_copilot deps — share wire shape with killmail_search so the
# OpenSearch client behaves identically across planes.
opensearch-py~=2.7
pymysql~=1.1
# LLM planner — only imported when the CLI / broker actually routes a
# question through the Claude planner. Pinned loosely so SDK security
# fixes land without manual bumps.
anthropic~=0.39
# Redis result cache — optional at runtime (broker works without it)
# but the image always ships the client so operators can enable the
# cache via env without rebuilding.
redis~=5.0
# Neo4j driver — path / neighbors intents project through the SDE
# universe graph. Optional at runtime (broker boots without it); we
# pin to match graph_universe_sync's driver line.
neo4j~=5.25
